datasource db {
  provider = "postgresql"
  url      = env("PIPELINE_PG_CONNSTRING")
}

// Models are defined via SQL migrations in prisma/migrations directories
// Use the migrate command to create new migrations:
// make new-migration name=your_migration_name
// This will create a timestamped directory with SQL files
